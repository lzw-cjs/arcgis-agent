# Plan 08-02: PyPI 包分发

## 目标

将 arcgis-agent 发布到 PyPI，让用户可以通过 `pip install arcgis-agent` 安装。

## 任务

### 1. 完善 pyproject.toml

添加/完善：readme、license、classifiers、keywords、urls、authors

### 2. 创建 MANIFEST.in

确保 README.md、LICENSE、CHANGELOG.md、docs/ 被打包

### 3. 添加 LICENSE 文件

MIT 许可证

### 4. 创建 CHANGELOG.md

### 5. 重写 README.md

面向 `pip install` 用户，重点标注 arcpy 依赖

### 6. 添加 GitHub Actions 工作流

`.github/workflows/publish.yml`：tag push 时自动发布到 PyPI

## 验证

- [ ] `python -m build` 成功生成 wheel 和 sdist
- [ ] `twine check` 无警告
- [ ] 安装后 `arcgis-agent --version` 可用