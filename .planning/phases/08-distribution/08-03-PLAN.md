# Plan 08-03: Web API 部署

## 目标

让 Web API 可以通过 Docker 或系统服务部署。

## 任务

### 1. 创建 Dockerfile + docker-compose.yml

含 arcpy 限制说明（需要 Windows 容器或 ArcGIS Pro 环境）

### 2. 创建 .env.example

### 3. pyproject.toml 添加 optional-dependencies

`[web]`、`[mcp]`、`[dev]` 分组

### 4. 创建部署文档

`docs/deployment.md`

### 5. 创建服务脚本

- Systemd 服务文件
- Windows 服务脚本

## 验证

- [ ] docker build 成功（或文档说明限制）
- [ ] .env.example 包含所有必要变量
- [ ] 部署文档步骤可执行