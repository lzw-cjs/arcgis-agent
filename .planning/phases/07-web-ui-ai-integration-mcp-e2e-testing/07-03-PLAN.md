---
phase: 07
plan: 07-03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/arcgis_agent/adapters/base.py
  - src/arcgis_agent/adapters/llm.py
  - src/arcgis_agent/adapters/mock_llm.py
  - src/arcgis_agent/adapters/gis_tools.py
  - src/arcgis_agent/config.py
autonomous: true
requirements:
  - D-23
  - D-24
  - D-26

must_haves:
  truths:
    - "系统支持多个 LLM 提供商(通义千问/DeepSeek/OpenAI)且切换时无需修改调用代码"
    - "OpenAICompatibleProvider 包装 ChatOpenAI，通过 base_url 连接国内模型"
    - "MockLLMProvider 返回预设响应，不调用外部 API"
    - "33 个 LangChain StructuredTool 包装现有 Service 层，LLM 可通过 bind_tools() 选择"
    - "LLMConfig 从环境变量加载多模型配置（Qwen/DeepSeek/OpenAI）"
  artifacts:
    - path: "src/arcgis_agent/adapters/base.py"
      provides: "ILLMProvider ABC（追加到现有 ABC 文件中）"
      contains: "class ILLMProvider(ABC)"
    - path: "src/arcgis_agent/adapters/llm.py"
      provides: "OpenAICompatibleProvider 生产实现（lazy init ChatOpenAI）"
      exports: ["OpenAICompatibleProvider"]
    - path: "src/arcgis_agent/adapters/mock_llm.py"
      provides: "MockLLMProvider 测试实现"
      exports: ["MockLLMProvider"]
    - path: "src/arcgis_agent/adapters/gis_tools.py"
      provides: "ALL_GIS_TOOLS — 33 个 LangChain StructuredTool"
      exports: ["ALL_GIS_TOOLS", "create_gis_tools"]
    - path: "src/arcgis_agent/config.py"
      provides: "LLMProviderConfig + LLMConfig dataclasses"
      exports: ["LLMProviderConfig", "LLMConfig"]
  key_links:
    - from: "src/arcgis_agent/adapters/llm.py"
      to: "langchain_openai.ChatOpenAI"
      via: "lazy init in llm property"
      pattern: "ChatOpenAI"
    - from: "src/arcgis_agent/adapters/gis_tools.py"
      to: "src/arcgis_agent/services/"
      via: "直接导入 Service 类"
      pattern: "from arcgis_agent\.services"
    - from: "src/arcgis_agent/config.py"
      to: "os.getenv"
      via: "LLMConfig.from_env() 读环境变量"
      pattern: "DASHSCOPE_API_KEY|DEEPSEEK_API_KEY|OPENAI_API_KEY"
---

<objective>
创建 AI 集成的底层：ILLMProvider Adapter 接口、OpenAI 兼容的生产实现、Mock 测试实现、33 个 LangChain StructuredTool 包装、多模型配置系统。

Purpose: 为 Wave 3 的 ChatService 和 chat endpoint 提供 LLM 调用和工具调度能力。遵循项目已有的 Adapter ABC + 生产实现 + Mock 实现的三层模式。

Output: 可切换多模型的 LLM Provider 系统、33 个 LangChain 工具定义、从环境变量加载的配置系统。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
现有 ABC 模式（本 plan 遵循此模式添加 ILLMProvider）：

```python
# src/arcgis_agent/adapters/base.py（现有）
class IGeoProcessor(ABC):
    @abstractmethod
    def buffer(self, ...) -> Path: ...
    @abstractmethod
    def clip(self, ...) -> Path: ...
    # ... 9 more methods

class IMapDocument(ABC):
    @abstractmethod
    def create_map(self, ...) -> Path: ...
    # ... 7 more methods

class ILayoutDocument(ABC):
    @abstractmethod
    def create_layout(self, ...) -> Path: ...
    # ... 2 more methods

class IDataAccessor(ABC):
    @abstractmethod
    def exists(self, ...) -> bool: ...
    # ... 10 more methods
```

```python
# src/arcgis_agent/config.py（现有）
CONFIG_DIR = Path.home() / ".arcgis-agent"
CONFIG_FILE = CONFIG_DIR / "config.json"

class WorkspaceConfig:
    def __init__(self, config_path=None): ...
    def get_workspace(self) -> Path | None: ...
    def set_workspace(self, path: Path) -> None: ...

# pyproject.toml（现有依赖）
dependencies = ["click>=8.1", "mcp>=1.0", "rich", "pydantic>=2.11"]
```

AI-SPEC.md 中的完整代码模板（Section 4.1 配置、Section 4.2 Provider、Section 4.3 工具定义）已经过验证。

**arcpy 线程安全约束（跨 plan 合约）：**
gis_tools.py 中的工具函数是同步的 LangChain StructuredTool，直接调用 Service 层。
调用方（07-04 ChatService）负责将这些工具调用包装在 `_run_in_thread()` (from `arcgis_agent.api.dependencies`)
或 `asyncio.to_thread()` 中，确保 arcpy COM 调用不阻塞 FastAPI 事件循环线程。
工具函数本身不需要关心线程安全——线程安全由调用方（agent loop）保证。
</interfaces>

@.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/07-CONTEXT.md
@.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/07-RESEARCH.md
@.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/AI-SPEC.md
@src/arcgis_agent/adapters/base.py
@src/arcgis_agent/config.py
@src/arcgis_agent/mcp_server.py
@src/arcgis_agent/services/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LLMConfig + ILLMProvider ABC + OpenAICompatibleProvider + MockLLMProvider</name>
  <files>
    src/arcgis_agent/config.py
    src/arcgis_agent/adapters/base.py
    src/arcgis_agent/adapters/llm.py
    src/arcgis_agent/adapters/mock_llm.py
  </files>
  <read_first>
    - src/arcgis_agent/adapters/base.py — [why: 在此文件追加 ILLMProvider ABC，遵循现有 4 个 ABC 的模式]
    - src/arcgis_agent/adapters/arcpy_adapter.py — [why: 生产实现的 lazy init 模式参考]
    - src/arcgis_agent/adapters/mock_adapter.py — [why: Mock 实现的返回模式参考]
    - src/arcgis_agent/config.py — [why: 在此文件追加 LLMConfig，遵循 WorkspaceConfig 的布局]
    - .planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/AI-SPEC.md — [why: Section 4.1/4.2 完整代码模板]
  </read_first>
  <behavior>
    - Test: LLMConfig.from_env() 从环境变量 DASHSCOPE_API_KEY 读取 Qwen 配置
    - Test: LLMConfig.from_env() 在未设置环境变量时 api_key 为空字符串，不崩溃
    - Test: OpenAICompatibleProvider.chat("hello") 返回 AIMessage（需要真实 API Key 时用 MockLLMProvider 替代）
    - Test: MockLLMProvider.chat("hello") 返回 AIMessage(content="Mock response")
    - Test: MockLLMProvider.chat_with_tools("buffer roads", ...) 返回 (AIMessage, list[dict])
    - Test: ILLMProvider ABC 无法直接实例化（abc.abstractmethod 保护）
  </behavior>
  <action>
    D-23（多模型支持）、D-24（ILLMProvider ABC）、D-26（OpenAI 兼容 API）的实现。

    1. **`src/arcgis_agent/config.py`** — 追加 LLMProviderConfig 和 LLMConfig。
       在文件末尾追加以下 dataclasses（保持现有 WorkspaceConfig 不变）：
       ```python
       from dataclasses import dataclass, field
       from typing import Optional
       import os

       @dataclass
       class LLMProviderConfig:
           provider: str                      # "qwen", "deepseek", "openai"
           model: str                         # API model ID: "qwen-plus", "deepseek-chat", "gpt-4o"
           base_url: str                      # OpenAI-compatible endpoint URL
           api_key: str                       # 从环境变量读取，绝不硬编码
           temperature: float = 0.1
           max_tokens: int = 4096
           timeout: int = 120
           max_retries: int = 2

       @dataclass
       class LLMConfig:
           default: str = "qwen"              # 默认使用哪个 provider
           providers: dict[str, LLMProviderConfig] = field(default_factory=dict)

           @classmethod
           def from_env(cls) -> "LLMConfig":
               return cls(
                   default=os.getenv("LLM_DEFAULT_PROVIDER", "qwen"),
                   providers={
                       "qwen": LLMProviderConfig(
                           provider="qwen",
                           model=os.getenv("QWEN_MODEL", "qwen-plus"),
                           base_url=os.getenv("QWEN_BASE_URL",
                               "https://dashscope.aliyuncs.com/compatible-mode/v1"),
                           api_key=os.getenv("DASHSCOPE_API_KEY", ""),
                       ),
                       "deepseek": LLMProviderConfig(
                           provider="deepseek",
                           model=os.getenv("DEEPSEEK_MODEL", "deepseek-chat"),
                           base_url=os.getenv("DEEPSEEK_BASE_URL",
                               "https://api.deepseek.com/v1"),
                           api_key=os.getenv("DEEPSEEK_API_KEY", ""),
                       ),
                       "openai": LLMProviderConfig(
                           provider="openai",
                           model=os.getenv("OPENAI_MODEL", "gpt-4o"),
                           base_url=os.getenv("OPENAI_BASE_URL",
                               "https://api.openai.com/v1"),
                           api_key=os.getenv("OPENAI_API_KEY", ""),
                       ),
                   },
               )

           def get_provider_config(self, name: str | None = None) -> LLMProviderConfig:
               name = name or self.default
               if name not in self.providers:
                   raise ValueError(f"Unknown provider '{name}'. Available: {list(self.providers)}")
               return self.providers[name]
       ```

    2. **`src/arcgis_agent/adapters/base.py`** — 追加 ILLMProvider ABC。
       在现有 4 个 ABC（IGeoProcessor, IMapDocument, ILayoutDocument, IDataAccessor）之后追加：
       ```python
       from langchain_core.messages import BaseMessage, AIMessage, SystemMessage, HumanMessage, ToolMessage  # noqa: F811

       class ILLMProvider(ABC):
           @abstractmethod
           def chat(self, user_message: str,
                    history: list[BaseMessage] | None = None) -> AIMessage: ...

           @abstractmethod
           def chat_with_tools(self, user_message: str,
                              history: list[BaseMessage] | None = None,
                              max_iterations: int = 5) -> tuple[AIMessage, list[dict]]: ...

           @abstractmethod
           def register_tools(self, tools: list) -> None: ...
       ```
       注意：`langchain_core.messages` 的导入放在文件顶部（与现有 `from abc import ABC, abstractmethod` 并列）。

    3. **`src/arcgis_agent/adapters/llm.py`** — OpenAICompatibleProvider 生产实现。
       按 AI-SPEC.md Section 4.2 完整模板实现，关键点：
       ```python
       from __future__ import annotations
       from langchain_openai import ChatOpenAI
       from langchain_core.messages import (BaseMessage, HumanMessage, AIMessage,
                                            SystemMessage, ToolMessage)
       from langchain_core.tools import BaseTool
       from arcgis_agent.adapters.base import ILLMProvider
       from arcgis_agent.config import LLMProviderConfig

       SYSTEM_PROMPT = """You are a GIS assistant with access to geoprocessing and mapping tools.
       ## Your capabilities
       - Create buffers, clips, intersections, unions, and dissolves
       - Export maps to PDF, PNG, and JPEG formats
       - Query layer metadata, field names, and feature counts
       - Reproject data between coordinate reference systems

       ## Rules
       - When asked to perform a GIS operation, ALWAYS use the appropriate tool
       - If a required parameter is missing, ask the user for clarification
       - After a tool completes, confirm the result with the output path
       - If a tool fails, explain why and suggest alternatives
       - GIS operations can be destructive (overwrite data). Confirm before overwriting
       - Output paths should use ASCII characters only. Use English directory names.
       - Respond in the same language as the user's message."""

       class OpenAICompatibleProvider(ILLMProvider):
           def __init__(self, config: LLMProviderConfig) -> None:
               self._config = config
               self._llm: ChatOpenAI | None = None  # lazy init
               self._tools: list[BaseTool] = []

           @property
           def llm(self) -> ChatOpenAI:
               if self._llm is None:
                   self._llm = ChatOpenAI(
                       model=self._config.model,
                       base_url=self._config.base_url,
                       api_key=self._config.api_key,
                       temperature=self._config.temperature,
                       max_tokens=self._config.max_tokens,
                       timeout=self._config.timeout,
                       max_retries=self._config.max_retries,
                   )
               return self._llm

           def register_tools(self, tools: list[BaseTool]) -> None:
               self._tools = tools

           def chat(self, user_message: str, history=None) -> AIMessage:
               messages = list(history) if history else []
               if not messages or not isinstance(messages[0], SystemMessage):
                   messages.insert(0, SystemMessage(content=SYSTEM_PROMPT))
               messages.append(HumanMessage(content=user_message))
               return self.llm.invoke(messages)

           def chat_with_tools(self, user_message: str, history=None, max_iterations=5):
               if not self._tools:
                   raise RuntimeError("No tools registered. Call register_tools() first.")
               llm_with_tools = self.llm.bind_tools(self._tools)
               messages = list(history) if history else []
               if not messages or not isinstance(messages[0], SystemMessage):
                   messages.insert(0, SystemMessage(content=SYSTEM_PROMPT))
               messages.append(HumanMessage(content=user_message))

               tool_log: list[dict] = []
               iteration = 0
               response = llm_with_tools.invoke(messages)

               while response.tool_calls and iteration < max_iterations:
                   iteration += 1
                   messages.append(response)
                   for tc in response.tool_calls:
                       tool_name = tc["name"]
                       tool_args = tc["args"]
                       tool_fn = {t.name: t for t in self._tools}[tool_name]
                       try:
                           result = tool_fn.invoke(tool_args)
                           tool_log.append({"name": tool_name, "args": tool_args,
                                           "result": str(result), "success": True})
                       except Exception as exc:
                           error_msg = f"Tool '{tool_name}' failed: {exc}"
                           tool_log.append({"name": tool_name, "args": tool_args,
                                           "result": error_msg, "success": False})
                           result = error_msg
                       messages.append(ToolMessage(content=str(result), tool_call_id=tc["id"]))
                   response = llm_with_tools.invoke(messages)
               messages.append(response)
               return response, tool_log
       ```

    4. **`src/arcgis_agent/adapters/mock_llm.py`** — MockLLMProvider 测试实现。
       ```python
       from __future__ import annotations
       from langchain_core.messages import AIMessage, BaseMessage, HumanMessage
       from arcgis_agent.adapters.base import ILLMProvider

       class MockLLMProvider(ILLMProvider):
           def __init__(self, responses=None):
               self._responses = responses or {}
               self._tools = []
               self._call_count = 0

           def register_tools(self, tools):
               self._tools = tools

           def chat(self, user_message, history=None):
               self._call_count += 1
               msg = self._responses.get("chat", "Mock response for: " + user_message)
               return AIMessage(content=msg)

           def chat_with_tools(self, user_message, history=None, max_iterations=5):
               self._call_count += 1
               tool_log = self._responses.get("tool_log", [])
               msg = self._responses.get("final", "Operation completed (mock).")
               return AIMessage(content=msg), tool_log
       ```

    5. **依赖安装**：确保 conda 环境中已安装 `langchain-core>=1.3` 和 `langchain-openai>=1.2`（根据 RESEARCH.md，已安装）。
  </action>
  <acceptance_criteria>
    - `grep -c "class ILLMProvider" src/arcgis_agent/adapters/base.py` 返回 1
    - `grep -c "class OpenAICompatibleProvider" src/arcgis_agent/adapters/llm.py` 返回 1
    - `grep -c "class MockLLMProvider" src/arcgis_agent/adapters/mock_llm.py` 返回 1
    - `grep -c "class LLMConfig" src/arcgis_agent/config.py` 返回 1
    - `grep -c "DASHSCOPE_API_KEY" src/arcgis_agent/config.py` 返回 1
    - `grep -c "from_env" src/arcgis_agent/config.py` 返回 >= 1
    - `grep -c "ChatOpenAI" src/arcgis_agent/adapters/llm.py` 返回 >= 1
    - `grep -c "SYSTEM_PROMPT" src/arcgis_agent/adapters/llm.py` 返回 1
    - `python -c "from arcgis_agent.adapters.llm import OpenAICompatibleProvider; from arcgis_agent.adapters.mock_llm import MockLLMProvider; from arcgis_agent.config import LLMConfig; print('All imports OK')"` 退出码 0
  </acceptance_criteria>
  <done>ILLMProvider ABC、生产/Mock 实现、LLMConfig 全部可用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 33 个 LangChain StructuredTool 包装 (gis_tools.py)</name>
  <files>
    src/arcgis_agent/adapters/gis_tools.py
  </files>
  <read_first>
    - src/arcgis_agent/mcp_server.py — [why: 33 个工具名称和参数签名，每个 MCP 工具映射为一个 StructuredTool]
    - src/arcgis_agent/adapters/base.py — [why: ILLMProvider.register_tools() 接收 BaseTool 列表]
    - src/arcgis_agent/services/ — [why: 每个工具在 invoke 时调用对应的 Service 方法]
    - .planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/AI-SPEC.md — [why: Section 4.3 完整工具定义模板]
  </read_first>
  <behavior>
    - Test: ALL_GIS_TOOLS 列表包含至少 33 个 StructuredTool 实例
    - Test: buffer_feature.invoke({"layer_name": "roads", "distance": 100, "unit": "meters"}) 调用 GeoprocessingService().buffer()
    - Test: 每个工具都有 name、description、args_schema（Pydantic）
    - Test: 工具名称与 MCP 工具名称一致（如 `gp_buffer`）
  </behavior>
  <action>
    D-04（工具覆盖）、D-26（OpenAI 兼容 API 工具 schema）的实现。

    **`src/arcgis_agent/adapters/gis_tools.py`** — 33 个 LangChain StructuredTool。
    创建工具的方式：每个 GIS 操作一个 `@tool(args_schema=...)` 装饰的函数，内部实例化对应 Service 并调用方法。

    ```python
    from __future__ import annotations
    from typing import Optional
    from pydantic import BaseModel, Field
    from langchain_core.tools import tool

    # ══ Workspace Tools ══

    class WorkspaceSetInput(BaseModel):
        path: str = Field(description="Workspace directory path containing ArcGIS Pro project files")

    @tool(args_schema=WorkspaceSetInput)
    def workspace_set(path: str) -> str:
        """Set the current workspace directory. Must contain ArcGIS Pro project files."""
        from arcgis_agent.services.workspace_service import WorkspaceService
        result = WorkspaceService().set_workspace(path)
        return f"Workspace set to: {path}. Success: {result.success}"

    class WorkspaceGetInput(BaseModel):
        pass

    @tool(args_schema=WorkspaceGetInput)
    def workspace_get() -> str:
        """Get the current workspace directory path."""
        from arcgis_agent.services.workspace_service import WorkspaceService
        result = WorkspaceService().get_workspace()
        return f"Current workspace: {result.data.get('workspace', 'not set') if result.data else 'not set'}"

    # ══ Project Tools ══
    # ... project_info

    # ══ Data Discovery Tools ══
    # ... data_list, data_describe, data_fields, data_extent, data_count

    # ══ Data Management Tools ══
    # ... data_copy, data_delete, data_rename, data_convert

    # ══ Geoprocessing Tools ══
    # ... gp_select, gp_clip, gp_buffer, gp_intersect, gp_union,
    # ... gp_dissolve, gp_spatial_join, gp_merge, gp_project

    class BufferInput(BaseModel):
        input_fc: str = Field(description="Input feature class path")
        output_fc: str = Field(description="Output feature class path")
        distance: float = Field(description="Buffer distance", gt=0)
        unit: str = Field(default="Meters", description="Distance unit: Meters, Kilometers, Feet, Miles")
        dissolve_field: Optional[str] = Field(default=None, description="Optional field to dissolve by")

    @tool(args_schema=BufferInput)
    def gp_buffer(input_fc: str, output_fc: str, distance: float,
                  unit: str = "Meters", dissolve_field: Optional[str] = None) -> str:
        """Create a buffer polygon around input features.
        Use this when the user asks to create a buffer zone, proximity area,
        or distance-based analysis around geographic features.
        """
        from arcgis_agent.services.geoprocessing import GeoprocessingService
        result = GeoprocessingService().buffer(
            input_fc=input_fc, output_fc=output_fc,
            distance=distance, unit=unit, dissolve_field=dissolve_field
        )
        return f"Buffer created: {result.data.get('output_path', output_fc)}. Feature count: {result.data.get('feature_count', 'N/A')}. Success: {result.success}"

    # ══ Map Tools ══
    # ... map_create, map_add_layer, map_remove_layer, map_list_layers,
    # ... map_set_extent, map_export, map_symbolize, map_label

    # ══ Layout Tools ══
    # ... layout_create, layout_add_element, layout_export

    # ══ Analysis Tools ══
    # ... analysis_summary_stats

    ALL_GIS_TOOLS = [
        workspace_set, workspace_get,
        project_info,
        data_list, data_describe, data_fields, data_extent, data_count,
        data_copy, data_delete, data_rename, data_convert,
        gp_select, gp_clip, gp_buffer, gp_intersect, gp_union,
        gp_dissolve, gp_spatial_join, gp_merge, gp_project,
        map_create, map_add_layer, map_remove_layer, map_list_layers,
        map_set_extent, map_export, map_symbolize, map_label,
        layout_create, layout_add_element, layout_export,
        analysis_summary_stats,
    ]
    # 精确工具数量以 mcp_server.py 中 @mcp.tool() 装饰器数量为准。
    # 此处使用宽松下限验证：>= 33 确保核心工具完整。
    assert len(ALL_GIS_TOOLS) >= 33, f"Expected >= 33 tools, got {len(ALL_GIS_TOOLS)}"
    ```

    注意：按 mcp_server.py 中 @mcp.tool() 实际数量创建工具。服务初始化时工具数量可能变化，使用 >= 33 下限保证核心覆盖。

    每个工具 description 必须包含：
    - 工具用途的一句话描述
    - 何时使用的提示（如"Use this when the user asks to..."）
    - 与相似工具的区分点（如 clip/intersect 的区别）

    每个 Input Schema 的 Field description 必须包含参数含义。

    **线程安全说明：** gis_tools.py 中的工具函数是同步的 LangChain StructuredTool，直接调用 Service 层。
    arcpy COM 线程安全由调用方（07-04 ChatService.stream_chat()）保证，
    调用方将通过 `asyncio.to_thread()` 将整个 agent 循环（含工具调用）在线程池中运行，
    共享 `dependencies._ARC_LOCK` 确保与 REST API 路径（07-02）互斥。
  </action>
  <acceptance_criteria>
    - `grep -v '^#' | grep -c "@tool" src/arcgis_agent/adapters/gis_tools.py` 返回 >= 33（等于 mcp_server.py 中 @mcp.tool() 的数量）
    - `grep -c "ALL_GIS_TOOLS" src/arcgis_agent/adapters/gis_tools.py` 返回 >= 1
    - `grep -c "args_schema" src/arcgis_agent/adapters/gis_tools.py` 返回 >= 33
    - `python -c "from arcgis_agent.adapters.gis_tools import ALL_GIS_TOOLS; print(f'{len(ALL_GIS_TOOLS)} tools'); assert len(ALL_GIS_TOOLS) >= 33, f'Expected >=33, got {len(ALL_GIS_TOOLS)}'"` 退出码 0
    - `python -c "from arcgis_agent.adapters.gis_tools import ALL_GIS_TOOLS; names = [t.name for t in ALL_GIS_TOOLS]; assert 'gp_buffer' in names; assert 'map_export' in names; assert 'data_list' in names; print('Key tools present')"` 退出码 0
  </acceptance_criteria>
  <done>ALL_GIS_TOOLS 包含全部 MCP 工具对应的 LangChain StructuredTool，可被 ILLMProvider.register_tools() 使用。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ILLMProvider -> External LLM API (HTTPS) | API Key 和用户消息发送到外部 LLM 服务 |
| gis_tools -> arcpy (COM) | 工具调用通过 Service 层 -> Adapter 层 -> arcpy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Information Disclosure | llm.py | mitigate | API Key 从环境变量读取，绝不硬编码（D-25）；SYSTEM_PROMPT 不含敏感信息 |
| T-07-09 | Elevation of Privilege | gis_tools.py | mitigate | 工具描述中明确规则：destructive operations require confirmation；LLM 判断是否应调用 |
| T-07-10 | Spoofing | llm.py | accept | LLM API 响应信任——HTTPS 传输，无客户端证书验证需求 |
| T-07-11 | Denial of Service | llm.py | mitigate | max_iterations=5（chat_with_tools），max_tokens=4096，timeout=120 |
</threat_model>

<verification>

```bash
# 验证所有导入
python -c "
from arcgis_agent.adapters.base import ILLMProvider
from arcgis_agent.adapters.llm import OpenAICompatibleProvider
from arcgis_agent.adapters.mock_llm import MockLLMProvider
from arcgis_agent.adapters.gis_tools import ALL_GIS_TOOLS
from arcgis_agent.config import LLMConfig, LLMProviderConfig

# 验证工具数（>= 33 下限）
print(f'Tools: {len(ALL_GIS_TOOLS)}')
assert len(ALL_GIS_TOOLS) >= 33

# 验证配置加载
config = LLMConfig.from_env()
print(f'Default provider: {config.default}')
print(f'Available: {list(config.providers)}')

# 验证 Mock 可用
mock = MockLLMProvider()
resp = mock.chat('hello')
assert resp.content

print('All OK')
"
```
</verification>

<success_criteria>
- `ILLMProvider` ABC 在 base.py 中定义，`chat()` 和 `chat_with_tools()` 和 `register_tools()` 三个抽象方法
- `OpenAICompatibleProvider` 通过 `base_url` 参数可连接任何 OpenAI 兼容端点
- `MockLLMProvider` 返回预设响应，不需要环境变量
- `ALL_GIS_TOOLS` 至少 33 个工具，每个有 name/description/args_schema
- `LLMConfig.from_env()` 从环境变量加载 3 个 provider 配置
</success_criteria>

<output>
完成后创建 `.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/07-03-SUMMARY.md`
</output>
