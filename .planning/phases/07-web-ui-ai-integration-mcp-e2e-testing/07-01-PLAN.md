---
phase: 07
plan: 07-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arcgis_agent/api/__init__.py
  - src/arcgis_agent/api/main.py
  - src/arcgis_agent/api/dependencies.py
  - src/arcgis_agent/api/middleware.py
  - src/arcgis_agent/api/schemas/__init__.py
  - src/arcgis_agent/api/schemas/chat.py
  - src/arcgis_agent/api/schemas/tasks.py
  - src/arcgis_agent/api/schemas/events.py
  - pyproject.toml
autonomous: true
requirements:
  - D-01
  - D-02
  - D-05
  - D-06
  - D-08
  - D-11
  - D-14

must_haves:
  truths:
    - "FastAPI服务在 http://127.0.0.1:8000 可启动"
    - "Swagger UI 在 /docs 可访问并列出所有已注册路由"
    - "_run_in_thread() 通过 threading.Lock 将 arcpy 调用串行化"
    - "API 响应使用 Result 模型 (success/code/message/data)"
    - "所有路由前缀为 /api/v1/"
  artifacts:
    - path: "src/arcgis_agent/api/main.py"
      provides: "FastAPI app factory with lifespan and CORS"
      exports: ["create_app"]
    - path: "src/arcgis_agent/api/dependencies.py"
      provides: "_run_in_thread() wrapper and DI functions"
      exports: ["_run_in_thread", "get_conversation_store"]
    - path: "src/arcgis_agent/api/middleware.py"
      provides: "Request metrics logging middleware"
      exports: ["metrics_middleware"]
  key_links:
    - from: "src/arcgis_agent/api/dependencies.py"
      to: "src/arcgis_agent/mcp_server.py"
      via: "threading.Lock"
      pattern: "_ARC_LOCK"
    - from: "src/arcgis_agent/api/main.py"
      to: "src/arcgis_agent/api/dependencies.py"
      via: "FastAPI lifespan"
      pattern: "lifespan"
---

<objective>
创建 FastAPI 应用骨架：app 工厂函数、依赖注入、请求 Schema 定义、CORS 配置、启动/关闭生命周期管理。

Purpose: 为 Wave 2 的工具路由和 Wave 3 的聊天端点提供 FastAPI 运行时基础。之后的全部 REST 工作都在此框架之上。

Output: 可启动的 FastAPI 应用（`python -m arcgis_agent.api.main` 启动后访问 `/docs` 即可看到 Swagger UI），所有路由以 `/api/v1/` 为前缀。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
现有资源（无需修改，直接引用）：

```python
# src/arcgis_agent/models/result.py
class Result(BaseModel):
    success: bool
    code: str = "OK"
    message: str = ""
    data: Optional[dict[str, Any]] = None
    warnings: list[str] = Field(default_factory=list)
    @classmethod def ok(cls, data=..., message=...) -> Result: ...
    @classmethod def error(cls, code, message, data=...) -> Result: ...
    @classmethod def from_exception(cls, exc: Exception) -> Result: ...

# src/arcgis_agent/mcp_server.py 中的模式（第32行、第37-57行）
_ARC_LOCK = threading.Lock()

def _run_sync(fn, *args, **kwargs) -> dict[str, Any]:
    try:
        with _ARC_LOCK:
            result = fn(*args, **kwargs)
            if isinstance(result, Result):
                return result.model_dump()
            return result
    except BrokenPipeError: raise
    except Exception as exc:
        return Result.from_exception(exc).model_dump()

# src/arcgis_agent/services/base.py
class BaseService:
    def __init__(self, gp=None, map_doc=None, data=None, layout_doc=None):
        self._gp = gp if gp is not None else self._make_gp()
        self._map = ...
        self._data = ...
        self._layout = ...
```

```python
# pyproject.toml 现有依赖
dependencies = ["click>=8.1", "mcp>=1.0", "rich", "pydantic>=2.11"]
[project.scripts]
arcgis-agent = "arcgis_agent.cli:main"
arcgis-agent-mcp = "arcgis_agent.mcp_server:main"
```
</interfaces>

@.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/07-CONTEXT.md
@.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/07-RESEARCH.md
@src/arcgis_agent/mcp_server.py
@src/arcgis_agent/models/result.py
@src/arcgis_agent/config.py
@src/arcgis_agent/services/base.py
@pyproject.toml
</context>

<tasks>

<!-- Task 1: Core infrastructure (main.py + dependencies.py + middleware.py) -->
<task type="auto" tdd="true">
  <name>Task 1: FastAPI app factory + _run_in_thread() + DI + middleware</name>
  <files>
    src/arcgis_agent/api/__init__.py
    src/arcgis_agent/api/main.py
    src/arcgis_agent/api/dependencies.py
    src/arcgis_agent/api/middleware.py
  </files>
  <read_first>
    - src/arcgis_agent/mcp_server.py — [why: 复用 _ARC_LOCK 和 _run_sync() 模式]
    - src/arcgis_agent/models/result.py — [why: 复用 Result 模型作为 API 响应格式]
    - src/arcgis_agent/config.py — [why: 遵循现有 WorkspaceConfig 文件布局和 JSON 读写模式]
    - pyproject.toml — [why: 需要添加 fastapi/uvicorn 依赖]
  </read_first>
  <behavior>
    - Test: _run_in_thread() 通过 asyncio.to_thread() + threading.Lock 执行函数
    - Test: create_app() 返回 FastAPI 实例，lifespan 正确初始化 ConversationStore
    - Test: metrics_middleware 记录请求路径、方法、状态码、耗时
    - Test: /docs 端点返回 200，Swagger UI 可见
    - Test: GET /api/v1/health 返回 {"status": "ok"}
  </behavior>
  <action>
    D-01（FastAPI 框架）、D-02（localhost only）、D-05（arcpy 线程安全）、D-08（api/ 嵌入现有包）、D-11（/api/v1/ 前缀）的核心实现。

    1. **`src/arcgis_agent/api/__init__.py`** — 空文件，让 api 成为包。

    2. **`src/arcgis_agent/api/dependencies.py`** — 核心基础设施。
       - 直接复用 `mcp_server.py` 第32行的 `_ARC_LOCK`（`import threading; _ARC_LOCK = threading.Lock()`），不重新创建新锁。
       - 实现 `async def _run_in_thread(fn, *args, **kwargs) -> dict[str, Any]`：
         - 内部定义 `def _sync() -> dict[str, Any]:` 闭包
         - 闭包内 `with _ARC_LOCK:` 获取锁
         - 闭包内 `lazy import Result`（`from arcgis_agent.models.result import Result`）
         - 调用 `result = fn(*args, **kwargs)`
         - 若 result 是 Result 实例则 `result.model_dump()`，否则直接返回
         - 异常时 `return Result.from_exception(exc).model_dump()`
         - 使用 `return await asyncio.to_thread(_sync)` 包装
       - 实现 `ConversationStore` 类（内存对话历史存储，key 为 session_id）
         - `__init__(self, max_sessions: int = 100)`：`self._store = OrderedDict()` + `self._lock = threading.Lock()`
         - `get(self, session_id: str) -> list[BaseMessage]`：加锁返回 `self._store.get(session_id, [])`
         - `update(self, session_id: str, messages: list[BaseMessage]) -> None`：加锁，LRU 淘汰（len >= max_sessions 时 `popitem(last=False)`），写入，`move_to_end(session_id)`
         - `delete(self, session_id: str) -> None`：加锁，pop session
         - 注意：ConversationStore 引用 `langchain_core.messages.BaseMessage` 类型，但此处仅声明类型提示，实际导入延迟到 ChatService 使用时
       - 创建全局单例 `_conversation_store: ConversationStore | None = None`
       - 实现 `def get_conversation_store() -> ConversationStore`：lazy init 全局单例（支持 lifespan 注入）

    3. **`src/arcgis_agent/api/main.py`** — FastAPI app 工厂。
       - 实现 `def create_app() -> FastAPI`：
         - `app = FastAPI(title="arcgis-agent REST API", version="0.1.0", docs_url="/docs", redoc_url=None)`
         - 添加 CORS 中间件（`CORSMiddleware`，`allow_origins=["http://localhost:5173"]`，`allow_methods=["*"]`，`allow_headers=["*"]`）
         - 添加 `metrics_middleware`
         - 定义 `@asynccontextmanager async def lifespan(app)`：
           - startup：初始化 `get_conversation_store()`（如果尚未初始化则创建）
           - shutdown：`get_conversation_store().delete()` 所有会话或 pass
         - `app.router.lifespan_context = lifespan`
         - 注册健康检查路由：`@app.get("/api/v1/health") async def health() -> dict` 返回 `{"status": "ok"}`
         - include_router 预留路由（tools、chat、tasks、upload），这些路由在 07-02/07-04 中创建
         - `return app`
       - 模块级 `app = create_app()` 用于 `uvicorn src.arcgis_agent.api.main:app`
       - 实现 `def main() -> None` 入口：
         - `import uvicorn; uvicorn.run(app, host="127.0.0.1", port=8000, log_level="info")`

    4. **`src/arcgis_agent/api/middleware.py`** — 请求指标中间件。
       - 实现 `async def metrics_middleware(request: Request, call_next)`：
         - `start = time.perf_counter()`
         - `response = await call_next(request)`
         - `duration_ms = (time.perf_counter() - start) * 1000`
         - 构造 `metrics = {"event": "request_metrics", "path": request.url.path, "method": request.method, "status": response.status_code, "duration_ms": round(duration_ms, 2)}`
         - status_code >= 500 → `logger.error("Server error", extra=metrics)`
         - duration_ms > 5000 → `logger.warning("Slow request", extra=metrics)`
         - 其余 → `logger.info("Request completed", extra=metrics)`
         - `return response`
  </action>
  <acceptance_criteria>
    - `grep -c "uvicorn\.run" src/arcgis_agent/api/main.py` 返回 >= 1
    - `grep -c "threading\.Lock" src/arcgis_agent/api/dependencies.py` 返回 >= 1
    - `grep -c "asyncio\.to_thread" src/arcgis_agent/api/dependencies.py` 返回 >= 1
    - `grep -c "CORSMiddleware" src/arcgis_agent/api/main.py` 返回 >= 1
    - `python -c "from src.arcgis_agent.api.main import create_app; app = create_app(); print(len(app.routes))"` 返回 >= 1（至少有 health 路由）
  </acceptance_criteria>
  <done>FastAPI 应用可启动，Swagger UI /docs 可访问，_run_in_thread() 串行化 arcpy 调用。</done>
</task>

<!-- Task 2: Schemas + pyproject.toml -->
<task type="auto" tdd="true">
  <name>Task 2: Pydantic schemas (chat/tasks/events) + pyproject.toml 更新</name>
  <files>
    src/arcgis_agent/api/schemas/__init__.py
    src/arcgis_agent/api/schemas/chat.py
    src/arcgis_agent/api/schemas/tasks.py
    src/arcgis_agent/api/schemas/events.py
    src/arcgis_agent/models/result.py
    pyproject.toml
  </files>
  <read_first>
    - src/arcgis_agent/models/result.py — [why: 复用 Result 模型规范]
    - src/arcgis_agent/api/dependencies.py — [why: 确认 _run_in_thread 依赖已有的 API 模块]
    - pyproject.toml — [why: 需要添加依赖和 entry point]
  </read_first>
  <behavior>
    - Test: ChatRequest 通过 Pydantic 验证（session_id 必填, message 必填, stream 默认 true）
    - Test: TaskCreate 通过 Pydantic 验证（tool_name 必填, arguments 默认 {}）
    - Test: TaskStatus 枚举包含 PENDING/RUNNING/COMPLETED/FAILED
    - Test: ProgressEvent/TokenEvent/ErrorEvent 通过 Pydantic 序列化
    - Test: pyproject.toml 中包含 arcgis-agent-web entry point
  </behavior>
  <action>
    D-06（Result 模型复用）、D-09（Task 模型）、D-14（Swagger 开启）的 Schema 层实现。

    1. **`src/arcgis_agent/api/schemas/__init__.py`** — 空文件。

    2. **`src/arcgis_agent/api/schemas/chat.py`** — 聊天请求/响应 Schema。
       ```python
       from pydantic import BaseModel, Field
       from typing import Optional

       class ChatRequest(BaseModel):
           session_id: str = Field(description="会话ID，前端生成UUID")
           message: str = Field(description="用户消息内容")
           stream: bool = Field(default=True, description="是否SSE流式响应")

       class ToolCallEvent(BaseModel):
           event: str = Field(description="事件类型: tool_start, tool_end")
           data: dict = Field(description="工具调用数据")
       ```

    3. **`src/arcgis_agent/api/schemas/tasks.py`** — 任务状态 Schema。
       ```python
       from pydantic import BaseModel, Field
       from typing import Optional
       from enum import Enum

       class TaskStatus(str, Enum):
           PENDING = "pending"
           RUNNING = "running"
           COMPLETED = "completed"
           FAILED = "failed"

       class TaskCreate(BaseModel):
           tool_name: str = Field(description="工具名称")
           arguments: dict = Field(default_factory=dict, description="工具参数")

       class TaskResult(BaseModel):
           task_id: str
           status: TaskStatus
           tool_name: str
           result: Optional[dict] = None
           error: Optional[str] = None
           progress: float = Field(default=0.0, ge=0.0, le=100.0)
           created_at: str
           updated_at: str
       ```

    4. **`src/arcgis_agent/api/schemas/events.py`** — SSE 事件模型。
       ```python
       from pydantic import BaseModel, Field
       from typing import Optional

       class ProgressEvent(BaseModel):
           event: str = "progress"
           data: dict = Field(description="{percent: float, message: str}")

       class TokenEvent(BaseModel):
           event: str = "token"
           data: dict = Field(description="{content: str}")

       class ErrorEvent(BaseModel):
           event: str = "error"
           data: dict = Field(description="{code: str, message: str}")
       ```

    5. **`pyproject.toml`** — 添加依赖和入口点。
       - 在 `[project] dependencies` 中添加：`"fastapi>=0.135", "uvicorn>=0.46", "python-multipart>=0.0.20", "sse-starlette>=3.3"`
       - 在 `[project.scripts]` 中添加：`arcgis-agent-web = "arcgis_agent.api.main:main"`

    6. **`src/arcgis_agent/models/result.py`** — 不修改。API 直接复用现有 Result 模型（D-06）。
  </action>
  <acceptance_criteria>
    - `grep -c "class ChatRequest" src/arcgis_agent/api/schemas/chat.py` 返回 >= 1
    - `grep -c "class TaskStatus" src/arcgis_agent/api/schemas/tasks.py` 返回 >= 1
    - `grep -c "class ProgressEvent" src/arcgis_agent/api/schemas/events.py` 返回 >= 1
    - `grep -c "arcgis-agent-web" pyproject.toml` 返回 >= 1
    - `grep -c "python-multipart" pyproject.toml` 返回 >= 1
    - `grep -c "sse-starlette" pyproject.toml` 返回 >= 1
    - `python -c "from arcgis_agent.api.schemas.chat import ChatRequest; from arcgis_agent.api.schemas.tasks import TaskCreate; from arcgis_agent.api.schemas.events import ProgressEvent; print('All schemas OK')"` 退出码 0
  </acceptance_criteria>
  <done>全部 Pydantic Schema 可用，pyproject.toml 包含 arcgis-agent-web 入口点和全部所需依赖。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> FastAPI (HTTP) | localhost 单用户，无外部网络暴露 |
| FastAPI -> arcpy (COM) | arcpy COM 调用必须在线程池中串行化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | main.py | mitigate | bind to 127.0.0.1 only（D-02），无外部网络暴露，disable CORS for all-origins in production |
| T-07-02 | Denial of Service | dependencies.py | mitigate | MAX_TOOL_ITERATIONS=5 guardrail（ChatService 中），max_tokens=2048（LLM config） |
| T-07-03 | Elevation of Privilege | dependencies.py | accept | threading.Lock 仅确保互斥，不提供业务授权——localhost 单用户，无权限升级风险 |
</threat_model>

<verification>

```bash
# 启动 FastAPI 服务
cd /path/to/arcgis-agent
python -m arcgis_agent.api.main &
sleep 3

# 验证 health endpoint
curl -s http://127.0.0.1:8000/api/v1/health

# 验证 Swagger UI
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/docs

# 停止服务
kill %1
```
</verification>

<success_criteria>
- `python -m arcgis_agent.api.main` 启动 FastAPI 服务，监听 127.0.0.1:8000
- `curl http://127.0.0.1:8000/api/v1/health` 返回 `{"status":"ok"}`
- `curl http://127.0.0.1:8000/docs` 返回 200（Swagger UI HTML）
- `arcgis-agent-web` entry point 可从命令行启动
- `pyproject.toml` 中包含 fastapi/uvicorn/python-multipart/sse-starlette 依赖
</success_criteria>

<output>
完成后创建 `.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/07-01-SUMMARY.md`
</output>
