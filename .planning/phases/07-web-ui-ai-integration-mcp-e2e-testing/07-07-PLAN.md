---
phase: 07
plan: 07-07
type: execute
wave: 4
depends_on:
  - 07-02
  - 07-03
  - 07-04
files_modified:
  - tests/e2e/test_mcp_tools.py
  - tests/e2e/test_chat_loop.py
  - tests/e2e/INTEGRATION_CHECKLIST.md
  - tests/e2e/conftest.py
  - tests/conftest.py
autonomous: true
requirements:
  - D-28
  - D-29
  - D-30
  - D-31

must_haves:
  truths:
    - "31 个 MCP 工具全部有对应的 E2E 测试用例"
    - "MCP E2E 测试通过 MCP stdio ClientSession 连接 MCP Server"
    - "手动测试清单 Markdown 中有明确的测试步骤和预期结果"
    - "E2E 测试复用 tests/conftest.py 中现有 fixture"
    - "聊天循环集成测试验证 ChatService 端到端流程"
  artifacts:
    - path: "tests/e2e/test_mcp_tools.py"
      provides: "31 个 MCP 工具的 E2E 测试"
    - path: "tests/e2e/test_chat_loop.py"
      provides: "ChatService 聊天循环集成测试"
    - path: "tests/e2e/INTEGRATION_CHECKLIST.md"
      provides: "Claude Code 手动测试清单（D-30）"
    - path: "tests/e2e/conftest.py"
      provides: "MCP ClientSession fixtures"
  key_links:
    - from: "tests/e2e/test_mcp_tools.py"
      to: "src/arcgis_agent/mcp_server.py"
      via: "MCP ClientSession stdio"
      pattern: "ClientSession"
    - from: "tests/e2e/conftest.py"
      to: "tests/conftest.py"
      via: "import fixtures"
      pattern: "from tests\.conftest"
---

<objective>
为全部 31 个 MCP 工具编写 E2E 测试（pytest + MCP ClientSession）、聊天循环集成测试、Claude Code 手动测试清单。

Purpose: 确保 MCP 工具通过 MCP 协议可被 Claude Code 正确调用，ChatService 端到端流程无 bug。为 D-30（手动测试清单）提供结构化指导。

Output: 可运行的 MCP E2E 测试套件（`pytest tests/e2e/ -v`）、手动测试清单、聊天循环集成测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
现有测试模式（本 plan 参考）：

```python
# tests/conftest.py（现有 fixtures）
@pytest.fixture
def mock_gp(): ...       # MockGeoProcessor
@pytest.fixture
def mock_map(): ...       # MockMapDocument
@pytest.fixture
def mock_data(): ...      # MockDataAccessor
@pytest.fixture
def mock_map_doc(): ...   # MockMapDocument (extended)
@pytest.fixture
def mock_layout(): ...    # MockLayoutDocument
@pytest.fixture
def workspace_config(tmp_path): ...  # WorkspaceConfig(temp)
@pytest.fixture
def workspace_service(workspace_config): ...  # WorkspaceService(temp)

# tests/e2e/test_e2e_english_path.py（现有 E2E 模式）
# - pytest fixture 设置 workspace
# - _require_arcpy() 在无 arcpy 时 skip
# - 模块级 fixture 创建/清理测试数据
# - KEEP_TEST_OUTPUT=1 保留测试输出文件
```

```python
# src/arcgis_agent/mcp_server.py — 31 个 @mcp.tool() 定义
# MCP ClientSession 使用方式（来自 MCP SDK）:
#   from mcp import ClientSession, StdioServerParameters
#   from mcp.client.stdio import stdio_client
```
</interfaces>

@.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/07-CONTEXT.md
@.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/07-RESEARCH.md
@.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/07-VALIDATION.md
@src/arcgis_agent/mcp_server.py
@src/arcgis_agent/adapters/mock_llm.py
@src/arcgis_agent/services/chat_service.py
@tests/conftest.py
@tests/e2e/test_e2e_english_path.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP E2E conftest.py + 31 tool E2E tests + Claude Code manual checklist</name>
  <files>
    tests/e2e/conftest.py
    tests/e2e/test_mcp_tools.py
    tests/e2e/INTEGRATION_CHECKLIST.md
  </files>
  <read_first>
    - src/arcgis_agent/mcp_server.py — [why: 31 个 MCP 工具名称和参数签名，每个工具一个 E2E 用例]
    - tests/conftest.py — [why: 复用现有 mock fixtures（D-31）]
    - tests/e2e/test_e2e_english_path.py — [why: 现有 E2E 测试模式参考：fixture 设置、workspace 管理、cleanup]
    - .planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/07-VALIDATION.md — [why: E2E 测试覆盖要求]
  </read_first>
  <action>
    D-28（pytest + MCP ClientSession）、D-29（31 工具覆盖）、D-30（手动测试清单）、D-31（复用 fixtures）的实现。

    1. **`tests/e2e/conftest.py`** — MCP 测试共享 fixtures。
       ```python
       """Shared fixtures for MCP E2E tests."""
       from __future__ import annotations

       import os
       import sys
       import subprocess
       import time
       from pathlib import Path

       import pytest
       from mcp import ClientSession, StdioServerParameters
       from mcp.client.stdio import stdio_client

       # ── Helpers ──

       def _require_mcp():
           """Check MCP SDK available."""
           try:
               import mcp  # noqa: F401
           except ImportError:
               pytest.skip("mcp SDK not available")

       def _require_arcpy():
           """Skip all tests if arcpy not available."""
           try:
               import arcpy  # noqa: F401
           except ImportError:
               pytest.skip("arcpy not available (requires ArcGIS Pro conda environment)")

       # ── MCP ClientSession Fixture ──

       @pytest.fixture
       async def mcp_session():
           """Create an MCP ClientSession connected to arcgis-agent-mcp via stdio."""
           _require_mcp()

           server_params = StdioServerParameters(
               command=sys.executable,
               args=["-m", "arcgis_agent.mcp_server"],
               env=os.environ.copy(),
           )

           async with stdio_client(server_params) as (read, write):
               async with ClientSession(read, write) as session:
                   await session.initialize()
                   yield session

       # ── Tool Discovery ──

       @pytest.fixture
       async def mcp_tools(mcp_session: ClientSession):
           """List all available MCP tools."""
           result = await mcp_session.list_tools()
           return {tool.name: tool for tool in result.tools}

       # ── Test Workspace Fixture ──

       @pytest.fixture(scope="module")
       def e2e_workspace():
           """Module-scope workspace for MCP E2E tests."""
           _require_arcpy()
           ws = Path("./test_e2e_workspace")
           ws.mkdir(exist_ok=True)
           # Set workspace via subprocess (CLI)
           yield ws
           # Cleanup unless KEEP_TEST_OUTPUT=1
           if os.environ.get("KEEP_TEST_OUTPUT") != "1":
               import shutil
               shutil.rmtree(str(ws), ignore_errors=True)
       ```

    2. **`tests/e2e/test_mcp_tools.py`** — 31 个工具的 E2E 测试。
       ```python
       """MCP E2E tests: verify all 31 tools are callable via MCP protocol.

       Run with: pytest tests/e2e/test_mcp_tools.py -v

       Note: Most tests are interact-only (verify tool exists and accepts params).
       Execution tests that require arcpy are marked with @pytest.mark.arcpy.
       """
       from __future__ import annotations
       import pytest
       from mcp import ClientSession

       pytestmark = pytest.mark.anyio

       # ── Tool Registration Tests ──

       class TestToolDiscovery:
           """Verify all expected tools are registered."""

           REQUIRED_TOOLS = [
               # Workspace
               "workspace_set", "workspace_get",
               # Project
               "project_info", "project_open", "project_save",
               # Data Discovery
               "data_list", "data_describe", "data_fields", "data_extent", "data_count",
               # Data Management
               "data_copy", "data_delete", "data_rename", "data_convert",
               # Geoprocessing
               "gp_select", "gp_clip", "gp_buffer", "gp_intersect",
               "gp_union", "gp_dissolve", "gp_spatial_join", "gp_merge", "gp_project",
               # Map
               "map_create", "map_add_layer", "map_remove_layer", "map_list_layers",
               "map_set_extent", "map_export", "map_symbolize", "map_label",
               # Layout
               "layout_create", "layout_add_element", "layout_export",
               # Analysis
               "analysis_summary_stats",
           ]

           async def test_all_tools_registered(self, mcp_tools):
               """Verify all 34 required tools are available."""
               for tool_name in self.REQUIRED_TOOLS:
                   assert tool_name in mcp_tools, f"Missing tool: {tool_name}"
               assert len(mcp_tools) >= len(self.REQUIRED_TOOLS)

           async def test_tool_has_description(self, mcp_tools):
               """Verify every tool has a description."""
               for name, tool in mcp_tools.items():
                   assert tool.description, f"Tool '{name}' has no description"

           async def test_tool_has_input_schema(self, mcp_tools):
               """Verify every tool has an input schema."""
               for name, tool in mcp_tools.items():
                   assert tool.inputSchema is not None, f"Tool '{name}' has no input schema"

       # ── Individual Tool Validation Tests ──

       class TestWorkspaceTools:
           async def test_workspace_set_schema(self, mcp_tools):
               tool = mcp_tools["workspace_set"]
               props = tool.inputSchema.get("properties", {})
               assert "path" in props, "workspace_set must have 'path' parameter"

           async def test_workspace_get_schema(self, mcp_tools):
               tool = mcp_tools["workspace_get"]
               assert tool.name == "workspace_get"

       class TestDataDiscoveryTools:
           async def test_data_list_schema(self, mcp_tools):
               tool = mcp_tools["data_list"]
               assert tool.inputSchema is not None

           async def test_data_describe_schema(self, mcp_tools):
               tool = mcp_tools["data_describe"]
               props = tool.inputSchema.get("properties", {})
               assert "dataset_path" in props

           async def test_data_fields_schema(self, mcp_tools):
               tool = mcp_tools["data_fields"]
               props = tool.inputSchema.get("properties", {})
               assert "dataset_path" in props

           async def test_data_extent_schema(self, mcp_tools):
               tool = mcp_tools["data_extent"]
               props = tool.inputSchema.get("properties", {})
               assert "dataset_path" in props

           async def test_data_count_schema(self, mcp_tools):
               tool = mcp_tools["data_count"]
               props = tool.inputSchema.get("properties", {})
               assert "dataset_path" in props

       class TestDataManagementTools:
           @pytest.mark.parametrize("tool_name", [
               "data_copy", "data_delete", "data_rename", "data_convert"
           ])
           async def test_data_mgmt_tool_exists(self, mcp_tools, tool_name):
               assert tool_name in mcp_tools

       class TestGeoprocessingTools:
           @pytest.mark.parametrize("tool_name", [
               "gp_select", "gp_clip", "gp_buffer", "gp_intersect",
               "gp_union", "gp_dissolve", "gp_spatial_join", "gp_merge", "gp_project"
           ])
           async def test_geoprocessing_tool_exists(self, mcp_tools, tool_name):
               assert tool_name in mcp_tools

           async def test_buffer_parameter_validation(self, mcp_tools):
               """Verify buffer tool has correct parameter schema."""
               tool = mcp_tools["gp_buffer"]
               props = tool.inputSchema.get("properties", {})
               assert "input_fc" in props
               assert "output_fc" in props
               assert "distance" in props
               assert "unit" in props

           async def test_clip_parameter_validation(self, mcp_tools):
               tool = mcp_tools["gp_clip"]
               props = tool.inputSchema.get("properties", {})
               assert "input_fc" in props
               assert "clip_fc" in props
               assert "output_fc" in props

           async def test_intersect_parameter_validation(self, mcp_tools):
               tool = mcp_tools["gp_intersect"]
               props = tool.inputSchema.get("properties", {})
               assert "inputs" in props
               assert "output_fc" in props

       class TestMapTools:
           @pytest.mark.parametrize("tool_name", [
               "map_create", "map_add_layer", "map_remove_layer", "map_list_layers",
               "map_set_extent", "map_export", "map_symbolize", "map_label"
           ])
           async def test_map_tool_exists(self, mcp_tools, tool_name):
               assert tool_name in mcp_tools

       class TestLayoutTools:
           @pytest.mark.parametrize("tool_name", [
               "layout_create", "layout_add_element", "layout_export"
           ])
           async def test_layout_tool_exists(self, mcp_tools, tool_name):
               assert tool_name in mcp_tools

       class TestAnalysisTools:
           async def test_summary_stats_exists(self, mcp_tools):
               assert "analysis_summary_stats" in mcp_tools

           async def test_summary_stats_schema(self, mcp_tools):
               tool = mcp_tools["analysis_summary_stats"]
               props = tool.inputSchema.get("properties", {})
               assert "input_fc" in props
               assert "output_table" in props
               assert "statistics_fields" in props
       ```

    3. **`tests/e2e/INTEGRATION_CHECKLIST.md`** — Claude Code 手动测试清单（D-30）。
       参考格式：
       ```markdown
       # arcgis-agent MCP Integration Checklist
       ## Claude Code Manual Tests

       ### Prerequisites
       - [ ] arcgis-agent MCP server configured in Claude Code MCP settings
       - [ ] ArcGIS Pro with valid license running
       - [ ] Test workspace with sample data available

       ### Test Cases

       #### 1. Workspace Management
       | # | Test Description | What to Say | Expected Result | Status |
       |---|-----------------|-------------|-----------------|--------|
       | 1.1 | Set workspace | "Set the workspace to D:/test_data" | Workspace set confirmation | [ ] |
       | 1.2 | Get workspace | "What is the current workspace?" | Returns the workspace path | [ ] |

       #### 2. Data Discovery
       | # | Test Description | What to Say | Expected Result | Status |
       |---|-----------------|-------------|-----------------|--------|
       | 2.1 | List datasets | "List all datasets in the workspace" | Returns list of feature classes | [ ] |
       | 2.2 | Describe dataset | "Describe the roads layer" | Returns metadata (type, CRS, field count) | [ ] |
       | 2.3 | Get fields | "What fields does the parcels layer have?" | Returns field names and types | [ ] |
       | 2.4 | Get count | "How many features in the buildings layer?" | Returns feature count | [ ] |

       #### 3. Data Management
       | # | Test Description | What to Say | Expected Result | Status |
       |---|-----------------|-------------|-----------------|--------|
       | 3.1 | Copy data | "Copy the roads layer to roads_backup" | Copy confirmation with path | [ ] |
       | 3.2 | Convert format | "Convert the parcels layer to GeoJSON" | Conversion confirmation | [ ] |

       #### 4. Geoprocessing
       | # | Test Description | What to Say | Expected Result | Status |
       |---|-----------------|-------------|-----------------|--------|
       | 4.1 | Buffer | "Create a 100 meter buffer around roads" | Buffer created with path and feature count | [ ] |
       | 4.2 | Clip | "Clip the parcels layer to the county boundary" | Clip result with feature count | [ ] |
       | 4.3 | Intersect | "Find where roads and flood zones intersect" | Intersection result with feature count | [ ] |
       | 4.4 | Select by attribute | "Select buildings where type is 'commercial'" | Selection result | [ ] |

       #### 5. Map Production
       | # | Test Description | What to Say | Expected Result | Status |
       |---|-----------------|-------------|-----------------|--------|
       | 5.1 | Export map | "Export the current map as PNG at 300 DPI" | PNG file created at specified path | [ ] |

       ### Test Completion Report
       - Date: ___________
       - Tester: ___________
       - Total Tests: ___
       - Passed: ___
       - Failed: ___
       - Notes:
       ```
  </action>
  <acceptance_criteria>
    - `grep -c "REQUIRED_TOOLS" tests/e2e/test_mcp_tools.py` 返回 >= 1
    - `grep -c "workspace_set\|workspace_get\|data_list\|gp_buffer\|map_export\|layout_create\|analysis_summary_stats" tests/e2e/test_mcp_tools.py` 返回 >= 7
    - `grep -c "ClientSession" tests/e2e/conftest.py` 返回 >= 1
    - `grep -c "stdio_client" tests/e2e/conftest.py` 返回 >= 1
    - `cat tests/e2e/INTEGRATION_CHECKLIST.md | grep -c "\[ \]"` 返回 >= 15（至少 15 个检查项）
    - `cat tests/e2e/INTEGRATION_CHECKLIST.md | grep -c "Test Description\|Expected Result\|Status"` 返回 >= 3
    - `pytest tests/e2e/test_mcp_tools.py -x -q --co` 列出的测试数 >= 31
  </acceptance_criteria>
  <done>31 个 MCP 工具 E2E 测试覆盖 (schema/existence)、Claude Code 手动测试清单就绪。</done>
</task>

<task type="auto">
  <name>Task 2: 聊天循环集成测试 (test_chat_loop.py)</name>
  <files>
    tests/e2e/test_chat_loop.py
  </files>
  <read_first>
    - src/arcgis_agent/services/chat_service.py — [why: ChatService.stream_chat() 测试目标]
    - src/arcgis_agent/adapters/mock_llm.py — [why: 集成测试使用 MockLLMProvider 无需 API Key]
    - src/arcgis_agent/api/dependencies.py — [why: ConversationStore 用于会话历史验证]
    - src/arcgis_agent/adapters/gis_tools.py — [why: ALL_GIS_TOOLS 中的工具调用需验证]
  </read_first>
  <action>
    D-28（pytest 测试）、D-31（复用 fixture）的进一步验证。

    **`tests/e2e/test_chat_loop.py`** — ChatService 端到端集成测试。
    ```python
    """Integration tests for the ChatService agent loop.

    Uses MockLLMProvider so no LLM API key is needed.
    Tests the full chain: user input -> LLM -> tool calls -> execution -> response.
    """
    from __future__ import annotations

    import pytest
    from langchain_core.messages import AIMessage

    from arcgis_agent.adapters.mock_llm import MockLLMProvider
    from arcgis_agent.adapters.gis_tools import ALL_GIS_TOOLS
    from arcgis_agent.services.chat_service import ChatService, TEMPLATE_SUGGESTIONS
    from arcgis_agent.api.dependencies import ConversationStore

    pytestmark = pytest.mark.anyio


    @pytest.fixture
    def mock_llm():
        """Mock LLM provider with configurable responses."""
        provider = MockLLMProvider(responses={
            "chat": "Hello! I'm your GIS assistant.",
        })
        provider.register_tools(ALL_GIS_TOOLS)
        return provider

    @pytest.fixture
    def conversation_store():
        return ConversationStore()

    @pytest.fixture
    def chat_service(mock_llm, conversation_store):
        return ChatService(
            llm_provider=mock_llm,
            store=conversation_store,
        )


    class TestChatServiceIntegration:
        """Integration tests for the ChatService agent loop."""

        async def test_simple_chat(self, chat_service):
            """User says hello, gets text response, no tools called."""
            events = []
            async for event in chat_service.stream_chat("test-session", "hello"):
                events.append(event)

            # Should have token and done events
            event_types = [e["event"] for e in events]
            assert "token" in event_types, f"Expected token in {event_types}"
            assert "done" in event_types, f"Expected done in {event_types}"

        async def test_chat_with_tools(self, mock_llm, conversation_store):
            """User requests buffer, LLM returns tool calls, tools execute."""
            # Configure mock to return tool calls
            mock_llm._responses = {
                "tool_log": [
                    {"name": "gp_buffer", "args": {"input_fc": "roads", "distance": 100, "unit": "Meters", "output_fc": "roads_buf"},
                     "result": "Buffer created: roads_buf. Feature count: 150. Success: True", "success": True}
                ],
                "final": "Buffer operation completed successfully.",
            }

            service = ChatService(llm_provider=mock_llm, store=conversation_store)
            events = []
            async for event in service.stream_chat("test-session-2", "buffer roads by 100m"):
                events.append(event)

            event_types = [e["event"] for e in events]
            assert "tool_start" in event_types
            assert "tool_end" in event_types
            assert "done" in event_types

        async def test_session_isolation(self, mock_llm, conversation_store):
            """Different sessions have independent conversation history."""
            mock_llm._responses = {"chat": "Response A"}
            service = ChatService(llm_provider=mock_llm, store=conversation_store)

            # Session 1
            events_1 = []
            async for e in service.stream_chat("s1", "msg1"):
                events_1.append(e)

            # Session 2
            events_2 = []
            async for e in service.stream_chat("s2", "msg2"):
                events_2.append(e)

            # Both should succeed independently
            assert any(e["event"] == "done" for e in events_1)
            assert any(e["event"] == "done" for e in events_2)

        async def test_suggestions_after_tool_execution(self, mock_llm, conversation_store):
            """Template suggestions are returned after tool execution."""
            mock_llm._responses = {
                "tool_log": [
                    {"name": "gp_buffer", "args": {"distance": 100}, "result": "OK", "success": True}
                ],
                "final": "Buffer done.",
            }

            service = ChatService(llm_provider=mock_llm, store=conversation_store)
            events = []
            async for event in service.stream_chat("test-s", "buffer"):
                events.append(event)

            suggestions_events = [e for e in events if e["event"] == "suggestions"]
            assert len(suggestions_events) >= 1, "Expected suggestions event"
            data = suggestions_events[0].get("data", {})
            items = data.get("items", [])
            assert any("叠加分析" in item for item in items), f"Expected buffer follow-up suggestion, got: {items}"

        async def test_error_handling(self, mock_llm, conversation_store):
            """ChatService handles errors gracefully with error SSE event."""
            mock_llm._responses = {
                "tool_log": [
                    {"name": "gp_buffer", "args": {}, "result": "Error: CRS mismatch", "success": False}
                ],
                "final": "The buffer operation failed due to CRS mismatch.",
            }

            service = ChatService(llm_provider=mock_llm, store=conversation_store)
            events = []
            async for event in service.stream_chat("test-e", "buffer"):
                events.append(event)

            tool_end_events = [e for e in events if e["event"] == "tool_end"]
            failed_tools = [e for e in tool_end_events if not e.get("data", {}).get("success")]
            assert len(failed_tools) >= 1, "Expected at least one failed tool"

        async def test_context_management(self, mock_llm, conversation_store):
            """Conversation history is trimmed when too long."""
            # Pre-populate store with many messages
            from langchain_core.messages import HumanMessage, AIMessage
            long_history = []
            for i in range(25):
                long_history.append(HumanMessage(content=f"msg {i}"))
                long_history.append(AIMessage(content=f"response {i}"))

            conversation_store.update("long-session", long_history)

            mock_llm._responses = {"chat": "OK"}
            service = ChatService(llm_provider=mock_llm, store=conversation_store)
            events = []
            async for event in service.stream_chat("long-session", "new message"):
                events.append(event)

            assert any(e["event"] == "done" for e in events), "Should complete with trim_messages"
    ```
  </action>
  <acceptance_criteria>
    - `grep -c "class TestChatServiceIntegration" tests/e2e/test_chat_loop.py` 返回 1
    - `grep -c "async def test_" tests/e2e/test_chat_loop.py` 返回 >= 5
    - `grep -c "MockLLMProvider" tests/e2e/test_chat_loop.py` 返回 >= 1
    - `grep -c "stream_chat" tests/e2e/test_chat_loop.py` 返回 >= 1
    - `grep -c "ConversationStore" tests/e2e/test_chat_loop.py` 返回 >= 1
    - `python -m pytest tests/e2e/test_chat_loop.py -x -q --co` 退出码 0
  </acceptance_criteria>
  <done>ChatService 集成测试覆盖：简单聊天、工具调用、会话隔离、模板建议、错误处理、上下文管理。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest -> MCP Server (stdio) | E2E 测试启动 MCP Server 子进程，通过 stdio 通信 |
| pytest -> MockLLMProvider | 集成测试使用 Mock，无外部 API 调用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-22 | Denial of Service | test_mcp_tools.py | accept | E2E 测试使用 MCP SDK ClientSession 标准协议，无资源耗尽风险 |
</threat_model>

<verification>

```bash
# 运行 MCP E2E 测试（需要 MCP SDK）
pytest tests/e2e/test_mcp_tools.py -v --co

# 运行聊天循环集成测试
pytest tests/e2e/test_chat_loop.py -v

# 验证手动测试清单格式
grep -c "\[ \]" tests/e2e/INTEGRATION_CHECKLIST.md
```
</verification>

<success_criteria>
- `tests/e2e/test_mcp_tools.py` 包含 31+ 个测试用例
- `tests/e2e/INTEGRATION_CHECKLIST.md` 包含按功能分组的完整手动测试步骤
- `tests/e2e/test_chat_loop.py` 包含至少 5 个集成测试场景
- 所有 E2E 测试可在有 MCP SDK 的环境中通过 `pytest tests/e2e/ -v` 运行
- 集成测试使用 MockLLMProvider，不依赖外部 API Key
</success_criteria>

<output>
完成后创建 `.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/07-07-SUMMARY.md`
</output>
