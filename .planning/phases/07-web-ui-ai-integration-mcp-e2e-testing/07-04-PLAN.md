---
phase: 07
plan: 07-04
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-03
files_modified:
  - src/arcgis_agent/services/chat_service.py
  - src/arcgis_agent/api/routes/chat.py
  - src/arcgis_agent/api/main.py
autonomous: true
requirements:
  - D-13
  - D-25
  - D-27

must_haves:
  truths:
    - "POST /api/v1/chat 接收用户消息，返回 SSE 流式响应"
    - "ChatService 编排 agent 循环：LLM 调用 -> 工具检测 -> 执行 -> 结果反馈 -> 最终回复"
    - "LLM API Key 仅在 ChatService 后端持有，前端不可见"
    - "工具执行后返回模板化建议（D-27）"
    - "SSE 事件类型：token、tool_start、tool_end、progress、error、done"
  artifacts:
    - path: "src/arcgis_agent/services/chat_service.py"
      provides: "ChatService agent 循环编排"
      exports: ["ChatService", "TEMPLATE_SUGGESTIONS"]
    - path: "src/arcgis_agent/api/routes/chat.py"
      provides: "POST /api/v1/chat SSE 端点"
      exports: ["router"]
  key_links:
    - from: "src/arcgis_agent/api/routes/chat.py"
      to: "src/arcgis_agent/services/chat_service.py"
      via: "FastAPI Depends(get_chat_service)"
      pattern: "get_chat_service"
    - from: "src/arcgis_agent/services/chat_service.py"
      to: "src/arcgis_agent/adapters/llm.py"
      via: "ILLMProvider.ainvoke() / bind_tools()"
      pattern: "llm_with_tools\.ainvoke"
    - from: "src/arcgis_agent/services/chat_service.py"
      to: "src/arcgis_agent/adapters/gis_tools.py"
      via: "ALL_GIS_TOOLS"
      pattern: "register_tools"
    - from: "src/arcgis_agent/services/chat_service.py"
      to: "src/arcgis_agent/api/dependencies.py"
      via: "asyncio.to_thread 包装 chat_with_tools"
      pattern: "asyncio\.to_thread"
---

<objective>
实现聊天代理循环（ChatService）和 SSE 流式聊天端点（POST /api/v1/chat）。将 ILLMProvider、31 个工具和 FastAPI SSE 流集成在一起。

Purpose: 完成 AI 集成层，前端可通过 POST /api/v1/chat 发送消息获得 AI 驱动的 GIS 工具执行结果。这是 Phase 7 的核心业务逻辑。

Output: 可用的 AI 聊天端点，支持工具调用、SSE 流式进度推送、模板化建议。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
07-03 创建的文件（本 plan 直接依赖）：

```python
# src/arcgis_agent/adapters/base.py → ILLMProvider ABC
class ILLMProvider(ABC):
    def chat(self, user_message, history=None) -> AIMessage: ...
    def chat_with_tools(self, user_message, history=None, max_iterations=5) -> tuple[AIMessage, list[dict]]: ...
    def register_tools(self, tools) -> None: ...

# src/arcgis_agent/adapters/gis_tools.py → ALL_GIS_TOOLS
ALL_GIS_TOOLS: list[BaseTool]  # 31+ LangChain StructuredTool

# src/arcgis_agent/adapters/llm.py → OpenAICompatibleProvider
class OpenAICompatibleProvider(ILLMProvider): ...

# src/arcgis_agent/adapters/mock_llm.py → MockLLMProvider
class MockLLMProvider(ILLMProvider): ...

# src/arcgis_agent/config.py → LLMConfig
@dataclass
class LLMConfig:
    default: str
    providers: dict[str, LLMProviderConfig]
    @classmethod def from_env() -> LLMConfig: ...
    def get_provider_config(self, name=None) -> LLMProviderConfig: ...

@dataclass
class LLMProviderConfig:
    provider, model, base_url, api_key, temperature, max_tokens, timeout, max_retries
```

07-01 创建的文件：

```python
# src/arcgis_agent/api/dependencies.py
async def _run_in_thread(fn, *args, **kwargs) -> dict[str, Any]: ...
    # 通过 asyncio.to_thread() + _ARC_LOCK 串行化执行
    # 返回 Result.model_dump() 或 dict

class ConversationStore:
    def get(session_id) -> list[BaseMessage]: ...
    def update(session_id, messages) -> None: ...
    def delete(session_id) -> None: ...

# src/arcgis_agent/api/schemas/chat.py
class ChatRequest(BaseModel): session_id: str; message: str; stream: bool = True

# src/arcgis_agent/api/schemas/events.py
class ProgressEvent, TokenEvent, ErrorEvent
```

**arcpy 线程安全合约（跨 plan 关键连接）：**
- REST API 路径（07-02 tools.py）：每个端点使用 `await _run_in_thread(_execute)` 包装单个 Service 调用
- AI Agent 路径（本 plan）：`chat_with_tools()` 是同步方法，内部遍历 LLM tool_calls 并同步调用 gis_tools.py 中的工具函数（进而调用 Service 层）
- 两个路径共享 `dependencies._ARC_LOCK`，需确保互斥
- 本 plan 的策略：`stream_chat()` 将同步的 `chat_with_tools()` 调用通过 `asyncio.to_thread()` 在线程池中运行，避免阻塞 FastAPI 事件循环，并使 arcpy COM 调用发生在独立线程中
- ChatService 不重复定义 `_run_in_thread`，直接使用 `arcgis_agent.api.dependencies._run_in_thread` 用于个别需线程安全的同步操作
</interfaces>

@.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/07-CONTEXT.md
@.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/07-RESEARCH.md
@.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/AI-SPEC.md
@src/arcgis_agent/adapters/base.py
@src/arcgis_agent/adapters/llm.py
@src/arcgis_agent/api/dependencies.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatService (agent loop + 模板化建议 + 护栏 + arcpy 线程安全)</name>
  <files>
    src/arcgis_agent/services/chat_service.py
  </files>
  <read_first>
    - src/arcgis_agent/adapters/llm.py — [why: OpenAICompatibleProvider to provide LLM]
    - src/arcgis_agent/adapters/gis_tools.py — [why: ALL_GIS_TOOLS to register]
    - src/arcgis_agent/adapters/base.py — [why: ILLMProvider interface]
    - src/arcgis_agent/api/dependencies.py — [why: ConversationStore for session history; _run_in_thread for arcpy thread safety]
    - .planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/AI-SPEC.md — [why: Section 4b.2 async agent loop 完整模板]
  </read_first>
  <behavior>
    - Test: ChatService.chat("hello") 通过 MockLLMProvider 返回 AIMessage 响应
    - Test: ChatService.chat("buffer roads by 100m") 当 Mock 返回 tool_calls 时执行工具并返回结果
    - Test: ChatService._get_suggestions 在 gp_buffer 执行后返回 "是否需要对这个缓冲区做叠加分析？"
    - Test: ChatService 在 tool_log 中记录每次工具调用（name, args, result, success）
    - Test: 护栏逻辑：路径含中文字符时记录 WARNING 日志但不阻塞
    - Test: 护栏逻辑：max_iterations 达到 5 时终止循环并返回摘要消息
    - Test: context_manager 在消息超过 20 条时裁剪到最近 15 条 + system prompt
  </behavior>
  <action>
    D-13（SSE 进度）、D-25（LLM 后端代理）、D-27（模板化建议）的实现。

    **`src/arcgis_agent/services/chat_service.py`** — 聊天代理循环编排。

    ```python
    from __future__ import annotations

    import asyncio
    import hashlib
    import json
    import logging
    import re
    from typing import AsyncGenerator, Optional

    from langchain_core.messages import BaseMessage, AIMessage, HumanMessage, SystemMessage, ToolMessage
    from langchain_core.messages.utils import trim_messages, count_tokens_approximately

    from arcgis_agent.adapters.base import ILLMProvider
    from arcgis_agent.adapters.gis_tools import ALL_GIS_TOOLS
    from arcgis_agent.api.dependencies import ConversationStore
    from arcgis_agent.config import LLMConfig

    logger = logging.getLogger(__name__)

    MAX_TOOL_ITERATIONS = 5
    MAX_CONTEXT_TOKENS = 80_000

    # ══ Template Suggestions (D-27) ══
    # 每个工具执行成功后，根据工具名称返回一条中文模板化建议
    TEMPLATE_SUGGESTIONS = {
        "gp_buffer": "是否需要对这个缓冲区做叠加分析？",
        "gp_clip": "是否需要查看裁剪后的属性表？",
        "gp_intersect": "是否需要导出求交结果为地图？",
        "gp_union": "是否需要对合并结果做融合处理？",
        "gp_dissolve": "是否需要对比融合前后的数据？",
        "gp_select": "是否需要对筛选结果做空间分析？",
        "gp_project": "是否需要导出投影变换后的数据？",
        "map_export": "是否需要调整符号化？",
        "data_convert": "是否需要对新格式的数据做空间分析？",
        "data_copy": "是否需要对复制的数据做处理？",
        # 通用建议（无特定工具匹配时使用）
        "_default": "是否需要对结果做进一步分析？",
    }

    class ChatService:
        def __init__(
            self,
            llm_provider: ILLMProvider,
            store: ConversationStore | None = None,
            llm_config: LLMConfig | None = None,
        ):
            self._provider = llm_provider
            self._store = store or ConversationStore()
            self._config = llm_config or LLMConfig.from_env()
            self._provider.register_tools(ALL_GIS_TOOLS)

        async def stream_chat(self, session_id: str, user_message: str) -> AsyncGenerator:
            """Stream chat response via SSE events.

            Yields SSE events as dicts: {event: str, data: dict}
            Event types: token, tool_start, tool_end, progress, error, done

            arcpy thread safety: chat_with_tools() runs via asyncio.to_thread()
            to prevent blocking the FastAPI event loop and to execute arcpy COM
            calls on a thread pool thread (sharing _ARC_LOCK from dependencies.py).
            """
            try:
                # 1. Load or create history
                history = self._store.get(session_id)

                # 2. Context management: trim if too long
                if len(history) > 20:
                    history = self._manage_context(history)

                # 3. Run agent loop in thread pool for arcpy thread safety
                #    chat_with_tools() is synchronous (calls llm.invoke() +
                #    tool.invoke() -> Service -> arcpy). Running via
                #    asyncio.to_thread() prevents blocking the event loop and
                #    ensures arcpy COM calls execute on a proper thread.
                response, tool_log = await asyncio.to_thread(
                    self._provider.chat_with_tools,
                    user_message,
                    history,
                    MAX_TOOL_ITERATIONS,
                )

                # 4. Emit tool execution events
                for tc in tool_log:
                    yield {
                        "event": "tool_start",
                        "data": {"name": tc["name"], "args": tc["args"]}
                    }
                    yield {
                        "event": "tool_end",
                        "data": {
                            "name": tc["name"],
                            "success": tc["success"],
                            "result": tc.get("result", "")[:500],  # truncate
                        }
                    }

                # 5. Update conversation history
                updated_history = self._build_updated_history(
                    history, user_message, response, tool_log
                )
                self._store.update(session_id, updated_history)

                # 6. Stream final text response token by token
                full_text = response.content if isinstance(response.content, str) else str(response.content)
                # For SSE streaming of text tokens, yield in chunks
                chunk_size = 50
                for i in range(0, len(full_text), chunk_size):
                    chunk = full_text[i:i+chunk_size]
                    yield {
                        "event": "token",
                        "data": {"content": chunk}
                    }
                    await asyncio.sleep(0.01)  # yield to event loop

                # 7. Suggestions based on last tool
                suggestions = self._get_suggestions(tool_log)
                if suggestions:
                    yield {
                        "event": "suggestions",
                        "data": {"items": suggestions}
                    }

                # 8. Done
                yield {
                    "event": "done",
                    "data": {
                        "tool_calls": len(tool_log),
                        "message_id": getattr(response, 'id', ''),
                    }
                }

            except Exception as exc:
                logger.exception("ChatService error: %s", exc)
                yield {
                    "event": "error",
                    "data": {"code": "CHAT_ERROR", "message": str(exc)}
                }

        # ══ Internal Methods ══

        def _build_updated_history(
            self,
            history: list[BaseMessage],
            user_message: str,
            response: AIMessage,
            tool_log: list[dict],
        ) -> list[BaseMessage]:
            """Build the new history list after a chat turn."""
            new_history = list(history) if history else []
            new_history.append(HumanMessage(content=user_message))
            # chat_with_tools already appends intermediate AIMessage + ToolMessage
            # for each iteration internally. We just append the final response.
            new_history.append(response)
            return new_history

        def _manage_context(self, messages: list[BaseMessage]) -> list[BaseMessage]:
            """Trim conversation history to fit context window."""
            try:
                return trim_messages(
                    messages,
                    strategy="last",
                    token_counter=count_tokens_approximately,
                    max_tokens=MAX_CONTEXT_TOKENS,
                    start_on="human",
                    end_on=("human", "tool"),
                    include_system=True,
                )
            except Exception:
                # Fallback: keep system + last 15 messages
                return [m for m in messages if isinstance(m, SystemMessage)] + messages[-15:]

        def _get_suggestions(self, tool_log: list[dict]) -> list[str]:
            """Get template suggestions based on the last successfully executed tool."""
            suggestions = []
            seen = set()
            for tc in reversed(tool_log):
                if tc.get("success"):
                    tool_name = tc["name"]
                    if tool_name in TEMPLATE_SUGGESTIONS and tool_name not in seen:
                        suggestions.append(TEMPLATE_SUGGESTIONS[tool_name])
                        seen.add(tool_name)
            if not suggestions:
                suggestions.append(TEMPLATE_SUGGESTIONS["_default"])
            return suggestions[:3]  # Max 3 suggestions
    ```

    **关键设计决策 — arcpy 线程安全：**
    - `chat_with_tools()` 是同步方法（ILLMProvider ABC 定义），内部通过 `tool_fn.invoke()` 同步调用 gis_tools.py 中的工具函数，进而调用 Service 层 → arcpy
    - `stream_chat()` 通过 `await asyncio.to_thread(...)` 将整个 `chat_with_tools()` 调用在线程池中执行
    - 这确保：arcpy COM 调用不在 FastAPI 事件循环线程中执行；多个并发请求在 thread pool 中排队（受 _ARC_LOCK 保护）
    - ChatService 不复制 `_run_in_thread` 静态方法（之前有重复定义但从未使用）。如需单个 Service 调用的线程安全包装，直接从 `arcgis_agent.api.dependencies` 导入 `_run_in_thread`
  </action>
  <acceptance_criteria>
    - `grep -c "class ChatService" src/arcgis_agent/services/chat_service.py` 返回 1
    - `grep -c "stream_chat" src/arcgis_agent/services/chat_service.py` 返回 >= 1
    - `grep -c "TEMPLATE_SUGGESTIONS" src/arcgis_agent/services/chat_service.py` 返回 >= 1
    - `grep -c "MAX_TOOL_ITERATIONS" src/arcgis_agent/services/chat_service.py` 返回 1
    - `grep -c "trim_messages" src/arcgis_agent/services/chat_service.py` 返回 >= 1
    - `grep -c "asyncio\.to_thread" src/arcgis_agent/services/chat_service.py` 返回 >= 1（arcpy 线程安全保障）
    - `grep -c "gp_buffer" src/arcgis_agent/services/chat_service.py` 返回 >= 1（TEMPLATE_SUGGESTIONS 中）
    - `python -c "from arcgis_agent.services.chat_service import ChatService; print('OK')"` 退出码 0
  </acceptance_criteria>
  <done>ChatService 编排完整的 Agent 循环，通过 asyncio.to_thread() 确保 arcpy 线程安全，返回 SSE 事件流。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/v1/chat SSE endpoint + Wiring</name>
  <files>
    src/arcgis_agent/api/routes/chat.py
    src/arcgis_agent/api/main.py
  </files>
  <read_first>
    - src/arcgis_agent/services/chat_service.py — [why: ChatService.stream_chat() SSE generator]
    - src/arcgis_agent/api/dependencies.py — [why: ConversationStore]
    - src/arcgis_agent/api/main.py — [why: 在此处 include_router]
    - src/arcgis_agent/api/schemas/chat.py — [why: ChatRequest Pydantic model]
    - src/arcgis_agent/config.py — [why: LLMConfig.from_env() for provider init]
  </read_first>
  <behavior>
    - Test: POST /api/v1/chat {"session_id":"test","message":"hello","stream":true} 返回 SSE text/event-stream
    - Test: SSE 流包含至少一个事件（token 或 error 或 done）
    - Test: POST /api/v1/chat with stream=false 返回 JSON response
    - Test: 连续两次 POST 同一 session_id，history 保留上下文
  </behavior>
  <action>
    D-13（SSE 推送）、D-25（LLM 后端代理）的最终实现。

    1. **`src/arcgis_agent/api/routes/chat.py`** — POST /api/v1/chat SSE 端点。
       ```python
       from __future__ import annotations

       import json
       import logging
       from typing import Optional

       from fastapi import APIRouter, Depends, HTTPException
       from fastapi.responses import StreamingResponse
       from sse_starlette.sse import EventSourceResponse

       from arcgis_agent.api.schemas.chat import ChatRequest
       from arcgis_agent.api.dependencies import get_conversation_store
       from arcgis_agent.config import LLMConfig
       from arcgis_agent.adapters.llm import OpenAICompatibleProvider
       from arcgis_agent.adapters.mock_llm import MockLLMProvider
       from arcgis_agent.services.chat_service import ChatService

       logger = logging.getLogger(__name__)
       router = APIRouter(prefix="/api/v1", tags=["Chat"])

       _chat_service: Optional[ChatService] = None
       _llm_config: Optional[LLMConfig] = None

       def get_chat_service() -> ChatService:
           global _chat_service, _llm_config
           if _chat_service is None:
               _llm_config = LLMConfig.from_env()
               provider_config = _llm_config.get_provider_config()
               
               # Use MockLLMProvider if no API key configured (dev safety)
               if not provider_config.api_key:
                   logger.warning("No LLM API key configured. Using MockLLMProvider.")
                   provider = MockLLMProvider()
               else:
                   provider = OpenAICompatibleProvider(provider_config)
               
               _chat_service = ChatService(
                   llm_provider=provider,
                   store=get_conversation_store(),
                   llm_config=_llm_config,
               )
           return _chat_service

       @router.post("/chat")
       async def chat_endpoint(body: ChatRequest):
           """Main chat endpoint.
           
           Returns SSE stream (text/event-stream) when stream=true.
           Returns JSON when stream=false.
           """
           service = get_chat_service()

           if not body.stream:
               # Non-streaming: collect all events and return JSON
               events = []
               async for event in service.stream_chat(body.session_id, body.message):
                   events.append(event)
               final_text = ""
               for evt in events:
                   if evt["event"] == "token":
                       final_text += evt["data"]["content"]
               return {
                   "session_id": body.session_id,
                   "response": final_text,
                   "events": [e for e in events if e["event"] in ("tool_start", "tool_end")],
               }

           # Streaming: SSE response
           async def event_generator():
               async for event in service.stream_chat(body.session_id, body.message):
                   yield {
                       "event": event["event"],
                       "data": json.dumps(event["data"], ensure_ascii=False),
                   }

           return EventSourceResponse(event_generator())

       @router.delete("/chat/{session_id}")
       async def clear_chat(session_id: str):
           """Clear a conversation session."""
           store = get_conversation_store()
           store.delete(session_id)
           return {"status": "ok", "session_id": session_id}

       @router.get("/chat/providers")
       async def list_providers():
           """List available LLM providers and which is active."""
           config = LLMConfig.from_env()
           providers_info = {}
           for name, pc in config.providers.items():
               providers_info[name] = {
                   "model": pc.model,
                   "configured": bool(pc.api_key),
                   "is_default": name == config.default,
               }
           return {
               "default": config.default,
               "providers": providers_info,
           }
       ```

    2. **`src/arcgis_agent/api/main.py`** — 更新 `create_app()`，添加 chat router：
       ```python
       from arcgis_agent.api.routes.chat import router as chat_router
       app.include_router(chat_router)
       ```
  </action>
  <acceptance_criteria>
    - `grep -c "EventSourceResponse" src/arcgis_agent/api/routes/chat.py` 返回 >= 1
    - `grep -c "get_chat_service" src/arcgis_agent/api/routes/chat.py` 返回 >= 1
    - `grep -c "MockLLMProvider" src/arcgis_agent/api/routes/chat.py` 返回 >= 1
    - `grep -c "include_router" src/arcgis_agent/api/main.py` 返回 >= 4（tasks + tools + upload + chat）
    - 启动 FastAPI 后 `curl -s -X POST http://127.0.0.1:8000/api/v1/chat -H "Content-Type: application/json" -d '{"session_id":"test","message":"hello","stream":false}'` 返回 JSON 响应（使用 MockLLMProvider）
    - `curl -s http://127.0.0.1:8000/api/v1/chat/providers` 返回 provider 列表
  </acceptance_criteria>
  <done>POST /api/v1/chat 端点响应 SSE 流式聊天，前端可连接使用。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> POST /api/v1/chat | 用户自然语言输入通过 HTTP 发送到 ChatService |
| ChatService -> LLM API | 用户消息 + 系统 prompt + 工具 schema 发送到外部 LLM |
| LLM -> ChatService | LLM 返回 tool_calls（JSON），ChatService 解析并执行 |
| ChatService -> arcpy (via asyncio.to_thread) | Agent 循环在线程池中执行，arcpy COM 调用与 REST API 路径共享 _ARC_LOCK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Information Disclosure | chat.py | mitigate | API Key 仅在后端存储（D-25），前端请求不包含任何密钥；未配置 API Key 时回退到 MockLLMProvider |
| T-07-13 | Elevation of Privilege | chat_service.py | mitigate | MAX_TOOL_ITERATIONS=5 防止无限工具调用循环；工具执行前 LLM 判断，拒绝明显的恶意路径（路径遍历） |
| T-07-14 | Information Disclosure | chat_service.py | accept | LLM 提示注入风险——用户可通过消息指导 LLM 行为，但 localhost 单用户无攻击面 |
| T-07-15 | Denial of Service | chat_service.py | mitigate | max_tokens=4096 限制每次响应，MAX_CONTEXT_TOKENS=80K 限制上下文 |
</threat_model>

<verification>

```bash
# 启动服务
python -m arcgis_agent.api.main &
sleep 3

# 验证 chat endpoint（MockLLMProvider 模式）
curl -s -X POST http://127.0.0.1:8000/api/v1/chat \
  -H "Content-Type: application/json" \
  -d '{"session_id":"test-001","message":"hello","stream":false}' | python -m json.tool

# 验证 SSE 流
curl -s -N -X POST http://127.0.0.1:8000/api/v1/chat \
  -H "Content-Type: application/json" \
  -d '{"session_id":"test-002","message":"buffer roads by 100m","stream":true}' | head -20

# 验证 provider 列表
curl -s http://127.0.0.1:8000/api/v1/chat/providers | python -m json.tool

kill %1
```
</verification>

<success_criteria>
- POST /api/v1/chat 返回 SSE 流 (text/event-stream)，包含 token/tool_start/tool_end/done 事件
- 未设置 API Key 时自动降级为 MockLLMProvider（dev 安全）
- 聊天历史按 session_id 隔离存储
- GET /api/v1/chat/providers 返回可用 LLM provider 列表
- 模板化建议在工具执行后出现在 SSE 事件中
</success_criteria>

<output>
完成后创建 `.planning/phases/07-web-ui-ai-integration-mcp-e2e-testing/07-04-SUMMARY.md`
</output>
