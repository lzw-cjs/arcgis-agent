---
phase: 00-setup
plan: 03
type: execute
wave: 2
depends_on: [00-01]
files_modified:
  - arcgis-agent.bat
autonomous: true
requirements: [ENV-03]

must_haves:
  truths:
    - "wrapper .bat 从干净 CMD 窗口可运行"
    - ".bat 脚本设置 PYTHONUTF8=1 和 PYTHONIOENCODING=utf-8"
    - ".bat 脚本先激活 proenv.bat 设置 DLL 路径，再 conda activate arcgis-agent 切换环境"
  artifacts:
    - path: "arcgis-agent.bat"
      provides: "Windows wrapper 脚本，激活 ArcGIS Pro 环境后调用 CLI"
      contains: "proenv.bat"
  key_links:
    - from: "arcgis-agent.bat"
      to: "proenv.bat"
      via: "call 激活 ArcGIS Pro Python 环境"
      pattern: "call.*proenv\\.bat"
    - from: "arcgis-agent.bat"
      to: "python -m arcgis_agent"
      via: "调用 CLI 模块"
      pattern: "python -m arcgis_agent"
---

<objective>
创建 wrapper .bat 脚本，使用户无需手动激活 conda 环境即可运行 arcgis-agent。

Purpose: 简化用户体验，从干净 CMD 窗口直接运行 arcgis-agent。
Output: arcgis-agent.bat 脚本。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-setup/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 arcgis-agent.bat wrapper 脚本</name>
  <files>arcgis-agent.bat</files>
  <read_first>
    - .planning/phases/00-setup/00-RESEARCH.md (Wrapper .bat 脚本模式、proenv.bat 路径、Pitfall #6/#7 编码问题)
  </read_first>
  <action>
在项目根目录创建 `arcgis-agent.bat`，内容如下：

```batch
@echo off
REM arcgis-agent.bat - Wrapper script for ArcGIS Pro Python environment
REM Usage: arcgis-agent.bat [args...]

REM Step 1: Activate proenv to set ARCGISHOME and DLL paths
call "C:\Program Files\ArcGIS\Pro\bin\Python\Scripts\proenv.bat"

REM Step 2: Switch to arcgis-agent conda env (where the package is installed)
call conda activate arcgis-agent

REM Set UTF-8 encoding for Chinese path support
set PYTHONUTF8=1
set PYTHONIOENCODING=utf-8

REM Run the CLI
python -m arcgis_agent %*
```

关键点：
- `proenv.bat` 设置 ARCGISHOME、PATH 等 DLL 路径，使 arcpy 可用
- `conda activate arcgis-agent` 切换到安装了 arcgis-agent 包的环境
- 两步激活：先设 DLL 路径（proenv），再切环境（conda activate）
- `PYTHONUTF8=1` 强制 Python 使用 UTF-8 编码，解决中文路径问题
- `PYTHONIOENCODING=utf-8` 确保 stdout/stderr 使用 UTF-8
- `%*` 传递所有命令行参数给 python -m arcgis_agent
  </action>
  <verify>
    <automated>cd "C:/Users/李打爷的电脑/Desktop/arcgis-agent" && python -c "
with open('arcgis-agent.bat', 'r') as f:
    content = f.read()
checks = [
    ('proenv.bat' in content, 'proenv.bat reference'),
    ('PYTHONUTF8=1' in content, 'PYTHONUTF8=1'),
    ('PYTHONIOENCODING=utf-8' in content, 'PYTHONIOENCODING=utf-8'),
    ('python -m arcgis_agent' in content, 'python -m arcgis_agent'),
    ('%*' in content, '%* parameter passthrough'),
]
for ok, name in checks:
    assert ok, f'Missing: {name}'
    print(f'OK: {name}')
print('All checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - `arcgis-agent.bat` 文件存在于项目根目录
    - 文件内容包含 `call "C:\Program Files\ArcGIS\Pro\bin\Python\Scripts\proenv.bat"`
    - 文件内容包含 `call conda activate arcgis-agent`
    - 文件内容包含 `set PYTHONUTF8=1`
    - 文件内容包含 `set PYTHONIOENCODING=utf-8`
    - 文件内容包含 `python -m arcgis_agent %*`
  </acceptance_criteria>
  <done>arcgis-agent.bat 已创建，包含 proenv 激活、UTF-8 编码设置和 CLI 调用</done>
</task>

<task type="auto">
  <name>Task 2: 验证 wrapper .bat 脚本可运行</name>
  <files></files>
  <read_first>
    - .planning/phases/00-setup/00-VALIDATION.md (手动验证方式)
  </read_first>
  <action>
从当前终端（已激活 arcgis-agent 环境）验证 .bat 脚本的基本语法正确性：

```bash
cd "C:/Users/李打爷的电脑/Desktop/arcgis-agent"
# 验证 .bat 文件存在且非空
test -s arcgis-agent.bat && echo "BAT file exists and is non-empty"

# 验证 proenv.bat 路径存在
test -f "C:/Program Files/ArcGIS/Pro/bin/Python/Scripts/proenv.bat" && echo "proenv.bat exists"
```

完整的端到端验证（从干净 CMD 窗口运行 `arcgis-agent.bat --version`）需要用户手动执行，因为：
1. 需要打开新的 CMD 窗口（干净环境）
2. .bat 脚本会激活 conda 环境

此步骤仅验证文件存在性和 proenv.bat 路径正确性。完整验证在 phase gate 的 success criteria 中覆盖。
  </action>
  <verify>
    <automated>cd "C:/Users/李打爷的电脑/Desktop/arcgis-agent" && test -s arcgis-agent.bat && test -f "C:/Program Files/ArcGIS/Pro/bin/Python/Scripts/proenv.bat" && echo "All verifications passed"</automated>
  </verify>
  <acceptance_criteria>
    - `arcgis-agent.bat` 文件存在且非空
    - `C:\Program Files\ArcGIS\Pro\bin\Python\Scripts\proenv.bat` 路径存在
  </acceptance_criteria>
  <done>arcgis-agent.bat 文件存在，proenv.bat 路径已确认可用</done>
</task>

</tasks>

<verification>
完成全部任务后，检查文件内容：

```bash
cat arcgis-agent.bat
```

预期输出应包含所有关键行（proenv.bat 调用、PYTHONUTF8、PYTHONIOENCODING、python -m arcgis_agent）。
</verification>

<success_criteria>
- arcgis-agent.bat 存在于项目根目录
- 脚本内容包含 proenv.bat 激活、UTF-8 编码设置、CLI 调用
- proenv.bat 路径已确认存在
- 用户可从干净 CMD 窗口运行 `arcgis-agent.bat --version`
</success_criteria>

<output>
After completion, create `.planning/phases/00-setup/00-03-SUMMARY.md`
</output>
